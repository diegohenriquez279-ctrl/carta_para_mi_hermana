node_modules
dist
.DS_Store
*.local
.vite

# Fotos originales sin renombrar (respaldo local, no se sube al repo)
_originales/

# Respaldo de las fotos antes de comprimirlas (no se sube al repo)
_originales-sin-comprimir/
